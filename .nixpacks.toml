[builds]
  [[builds.frontend]]
  path = "FrontEnd"
  build = "npm install"
  output = "build"

  [[builds.backend]]
  path = "BackEnd"
  build = "npm install"
  output = "dist"

[deploy]
  backend = "BackEnd/server.js"
  port = 3000
